# build ng
ng build
# build docker image
docker build -t lab-manager-ui . 
# run docker image
docker run -d -p 4400:80 lab-manager-ui

# stop docker image
docker run -d -p 4400:80 lab-manager-ui

#get all active images
docker ps -a

#remove image
docker rm <container id>